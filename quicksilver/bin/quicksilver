#!/usr/bin/env ruby

lib_path = File.expand_path('../lib', __dir__)
$LOAD_PATH.unshift(lib_path) unless $LOAD_PATH.include?(lib_path)

require 'main'

# captura Ctrl+C para sair bonitinho
trap("SIGINT") do
  puts "\n\nEncerrando Quicksilver... Até mais!"
  exit
end

QuicksilverApp.new(ARGV).run
